ANTHROPIC_API_KEY=         # Claude for extraction, reconciliation, procurement agent, RAG alerts
DATABASE_URL=              # Neon PostgreSQL connection string (optional — runs without DB in demo mode)
